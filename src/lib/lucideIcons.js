// Web shim: the native app imported icons from this module (a wrapper over
// @expo/vector-icons). On the web we just re-export the real lucide-react
// components, whose names match lucide's PascalCase convention.
export * from 'lucide-react';
